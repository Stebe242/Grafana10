# # config file version
apiVersion: 1

apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: modelfactory
    url: http://tasks.influxdb:8086
    isDefault: true
    timeInterval: 10s
